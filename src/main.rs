pub mod Generics; 
pub mod Closures;


fn main() {
    // let value = Generics::Struct::Struct::f('b', 1, 2);
    // println!("{}", value);

    // Generics::Enum::Enum::show_divide2();
    Closures::Closures::Closures();

}
